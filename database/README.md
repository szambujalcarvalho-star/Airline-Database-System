# Base database schema

`aviacao.sql` is the starter PostgreSQL schema supplied by the Databases course for Part 2 of the project.

It is included here because the submitted implementation depends on this schema and it makes the repository reproducible. The schema is **course-provided material**, not student-authored code.

The assignment PDF and blank notebook template are intentionally not redistributed in this repository.
